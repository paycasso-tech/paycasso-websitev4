⦁ API folder in app>(main)>api to be deleted
⦁ Remove API call and keep it in constant.ts
⦁ We have 2 folder sturucters right now like app>main we need to combine it like the experience and how it works needs to be inside in (main) ;
⦁ (main) ke sub folders ko bahar nikalna hai at level of experience and how it works
⦁ in Components hume app folder ko rename krna hai ; folder structure according to the hierarchy like home>navbar>navbar components
⦁ Common UI components
⦁ Remove Supabase Call ; its mostly inside lib (dont remove completely just comment out as of now)
⦁ Remove WEB3 related, Circle Related , Backend related code as we have shifted everything to Express backend
⦁ Dont remove relevant constants and fonts which are used on platform
⦁ lib_cb to be removed completely
